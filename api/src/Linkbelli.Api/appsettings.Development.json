{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      // Every statement, at Information, from sweeps that run every minute — so the log never
      // goes quiet and finding one exception in it means grepping. Turn this back down to
      // Information when you actually want to read the SQL.
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5433;Database=linkbelli;Username=linkbelli;Password=linkbelli"
  },
  "Quota": {
    "DefaultMaxRunsPerDay": 200
  }
}
